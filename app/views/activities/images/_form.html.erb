<%= simple_form_for [@project, @activity, @image] do |f| %>
  <%= f.input :description %>
  <%= f.input :image_file, input_html: { multiple: true, name: "image[image_file]" } %>
  <%= submit_button %>
  <%= button_form_to_back "Volver", [@project, @activity, :images] %>
<% end %>

